[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[phases.install]
cmds = ["/opt/venv/bin/pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
